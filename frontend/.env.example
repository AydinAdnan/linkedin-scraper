# Base URL of the LinkedIn Profile API backend
VITE_API_URL=http://localhost:4000

# Must match the backend's API_KEY, if one is set. This ends up visible in
# the built JS bundle — it deters casual abuse, it is not a real secret.
VITE_API_KEY=
